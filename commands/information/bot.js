module.exports = {
    name: "bots",
    aliases: ["robots"],
    category: "information",
    description: "Shows all the bots in the server",
    run: async (client, message, args) => {
        message.channel.send(`Command is Disabled For Improvement`);
    }
}